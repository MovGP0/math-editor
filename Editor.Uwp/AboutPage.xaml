<Page xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="Editor.AboutWindow" MinHeight="200" MinWidth="200" x:Name="This">
    <Grid DataContext="{Binding ElementName=This}" HorizontalAlignment="Center" VerticalAlignment="Bottom">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="{Binding Version}" FontFamily="Noto" FontSize="14" Margin="30,20,0,0" />
        <Grid Grid.Column="1" Grid.Row ="0" Width="80">
            <Image Source="app_icon.ico" HorizontalAlignment="Right" />
        </Grid>
    </Grid>
</Page>